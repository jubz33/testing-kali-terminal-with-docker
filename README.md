# Kali Docker Terminal

With Docker running, open a terminal in this project folder and use Docker Compose to start the Kali terminal:

```sh
docker compose run --build --rm kali
```

Type `exit` to close the Kali terminal.
